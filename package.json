{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "HOST=0.0.0.0 nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "clean": "rimraf node_modules yarn.lock package-lock.json .nuxt pnpm-lock.yaml"
  },
  "devDependencies": {
    "nuxt": "^3.3.1"
  },
  "dependencies": {
    "@quasar/extras": "^1.15.11",
    "nuxt-quasar-ui": "^1.0.3",
    "quasar": "^2.11.7",
    "rimraf": "^4.4.0"
  }
}
